# Changelog

## Unreleased

### Added

* Added `iterate_callback` and `single_callback` higher order function decorators.
* Added `buffer_maxsize` to `iterate` and `generate`.
* Added python 3.14 support metadata.

### Changed

* Updated build system to poetry >=2.
